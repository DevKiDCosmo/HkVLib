# Data Integrety.

Show if configs are loaded correctly and check if all values and setted vars/symbols are correct and existencial. This doesn't check flags and statuses of daemons and taks. This will be done by init ut that runs at last.